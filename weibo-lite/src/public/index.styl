@-moz-document domain("weibo.com") {
  // 公共样式  注意：下面一定要为 /*![[defaultTheme]]*/
  /*![[defaultTheme]]*/
  @import 'base'
  @import 'header'
  @import 'button'
  @import 'background'
  @import 'feed'
}